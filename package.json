{
  "name": "kglacer-macro",
  "version": "1.1.7",
  "license": "MIT",
  "type": "module",
  "sideEffects": false,
  "scripts": {
    "start": "bun src/build.ts",
    "lint": "stylelint \"src/**/*.{css,scss,sass}\" --fix && eslint \"src/**/*{.js,.ts}\" --fix && tsc",
    "test": "bun test",
    "version:sync": "bun scripts/manage-version.ts --sync",
    "version:bump": "bun scripts/manage-version.ts patch"
  },
  "devDependencies": {
    "@softsky/configs": "^1.3.5",
    "@types/bun": "^1.3.11"
  },
  "dependencies": {
    "@softsky/utils": "^2.8.4"
  }
}
